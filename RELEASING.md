<!-- Template: replace the body of this file with this repository's release process once it is
     defined. Cover: how a release branch is cut, how a release candidate is promoted to final,
     how the changelog is assembled, and what publishes the packages (workflow, registry,
     approval gate). Sibling libraries to model on: `compact-contracts` (automated,
     approval-gated) and `cairo-contracts` (manual checklist). Then delete this comment. -->

# Releasing

The release process for this repository is defined and validated as part of preparing the first
release. It will cover release branches, release-candidate promotion, changelog assembly and
package publication.

Whatever the process: install from tagged releases only, never from `main`.
