<!-- Template: at day zero this file says the code is unaudited. When the first report lands,
     delete the warning below, uncomment the OPTIONAL block, replace the example rows with the
     real one, and remove this comment. Keep the table current afterwards: one row per report,
     newest at the bottom. -->

# Audits

> [!WARNING]
> This library has not been audited yet.

<!-- OPTIONAL — uncomment with the first audit report. Delete the warning above at the same time.

Every security review of this repository, tied to the exact code it covered. Report files live
alongside this index, named `YYYY-MM-<target>-<scope>.pdf`.

The two example rows show the two shapes a row takes: a full audit pinned to one commit, and a
diff audit pinned to a compare range.

| Date | Target | Commit | Auditor | Scope | Links |
| ---- | ------ | ------ | ------- | ----- | ----- |
| {{Month YYYY}} | v{{X.Y.Z}} | [`{{short-sha}}`](https://github.com/OpenZeppelin/{{repo}}/tree/{{full-sha}}) | {{Auditor}} | {{what was in scope, e.g. "v{{X.Y.Z}} release audit" or a named module}} | [🔗](./{{YYYY-MM}}-v{{X.Y.Z}}.pdf) |
| {{Month YYYY}} | v{{X.Y.Z}} | [`{{sha-a}} → {{sha-b}}`](https://github.com/OpenZeppelin/{{repo}}/compare/{{sha-a}}...{{sha-b}}) | {{Auditor}} | {{what changed between the two commits}} | [🔗](./{{YYYY-MM}}-v{{X.Y.Z}}-diff.pdf) |

-->
